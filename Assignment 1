
                                        Assignment-1
1.	Values		: ‘hello’ -87.8, 6
    Expression		: *, -, /, +
2.	String is a value which is stored in the text and variable is the text which stores the value.
3.	Integer – It represents some range of mathematical integers.
    String – It is used to represent text rather than numbers and it can also contain spaces and numbers.
    Character- This stores strings of letters, numbers, and symbols.
4.	An expression is a combination of values, variables, operators, and calls to functions. Expressions need to be evaluated. If u ask the expression to print it evaluates and prints the result. It is an instruction that combines values and operators and always evaluate down to a single value.
5.	Evaluation of expression always produces a result value but Execution may or may not produce a result value, it only does whatever the statement says.
6.	After running the code the variable bacon contain the string value of 22 which is stored in it.
7.	The values of the two terms are same. Output= ‘spamspamspam’
8.	Because eggs is text which is valid and stored by variable but 100 is an integer which is only stored by string so it is invalid for the variable. So variable can store string but string cannot store the variable.
9.	Int() , float() , and str() .
10.	It can only execute string to string but not integer. we can execute this expression by writing this in the following expression.
‘I have eaten 99 burritos’
